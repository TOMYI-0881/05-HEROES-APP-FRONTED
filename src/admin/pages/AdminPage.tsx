/**
 * Página de administración (placeholder).
 * Ruta: /admin
 * Actualmente solo muestra un texto, pendiente de implementar funcionalidad.
 */
const AdminPage = () => {
  return <div>AdminPage</div>;
};

export default AdminPage;
